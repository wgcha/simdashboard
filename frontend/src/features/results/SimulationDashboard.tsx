import { useEffect, useRef, useState } from 'react'
import { Bar, BarChart, CartesianGrid, Cell, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { AlertTriangle, Expand, Image as ImageIcon, Layers3, Pause, Play, RotateCcw } from 'lucide-react'
import { simulationDashboardApi, type DashboardAsset, type DashboardCatalog, type DashboardChoice, type DashboardComparisonMember, type DashboardDistribution, type DashboardEdgePeak, type DashboardMember, type DashboardScene, type DashboardSceneDetail, type DashboardValue, type UsageDashboard } from '../../shared/api/simulationDashboard'
import './SimulationDashboard.css'
import { SimulationLocationMap } from './SimulationLocationMap'

type Props = { projectId: string; requestId: string }
type Tab = 'usage' | 'distribution'
const EDGES = ['TOP', 'BOTTOM', 'LEFT', 'RIGHT'] as const
const ROLES = ['CELL', 'CUSHION', 'BOX'] as const
const COLORS = ['var(--color-chart-series-1)', 'var(--color-chart-series-2)', 'var(--color-chart-series-3)', 'var(--color-chart-series-4)']

function errorText(reason: unknown) { return reason instanceof Error ? reason.message : '대시보드 결과를 불러오지 못했습니다.' }
function assetUrl(asset: DashboardAsset) { return asset.url || simulationDashboardApi.assetUrl(asset.asset_id) }
function valueText(value?: DashboardValue | null) { return value?.value == null ? '값 없음' : `${value.value}${value.unit ? ` ${value.unit}` : ' · 단위 미확인'}` }
function missingText(status?: string | null, reason?: string | null) { return reason || status || '자료 없음' }
function SelectField({ label, value, choices, onChange, disabled = false }: { label: string; value: string; choices: DashboardChoice[]; onChange: (value: string) => void; disabled?: boolean }) {
  return <label className="simulation-dashboard__select"><span>{label}</span><select value={value} disabled={disabled} onChange={(event) => onChange(event.target.value)}><option value="">선택</option>{Array.from(new Map(choices.map((choice) => [choice.id, choice])).values()).map((choice) => <option key={choice.id} value={choice.id} disabled={choice.disabled} title={choice.reason ?? undefined}>{choice.label}</option>)}</select></label>
}

export function SimulationDashboard({ projectId, requestId }: Props) {
  const [tab, setTab] = useState<Tab>('usage')
  const [catalog, setCatalog] = useState<DashboardCatalog | null>(null)
  const [catalogError, setCatalogError] = useState('')
  const [caseId, setCaseId] = useState('')
  const [captureId, setCaptureId] = useState('')
  const [loadCaseId, setLoadCaseId] = useState('')
  const [runId, setRunId] = useState('')
  const [mode, setMode] = useState('')
  const [componentId, setComponentId] = useState('')
  const [basis, setBasis] = useState<'' | 'REPORTED_SUMMARY' | 'DETAIL'>('')
  const [referenceCaseId, setReferenceCaseId] = useState('')
  const [referenceCaptureId, setReferenceCaptureId] = useState('')
  const [catalogRevision, setCatalogRevision] = useState(0)
  const latestCatalogKey = useRef('')

  useEffect(() => {
    const controller = new AbortController(); const key = `${requestId}:${tab}`; latestCatalogKey.current = key
    setCatalog(null); setCatalogError(''); setReferenceCaseId(''); setReferenceCaptureId(''); setBasis(''); setCaseId(''); setLoadCaseId(''); setRunId(''); setMode(''); setCaptureId(''); setComponentId('')
    simulationDashboardApi.catalog(requestId, tab === 'usage' ? 'USAGE' : 'DISTRIBUTION', controller.signal).then((next) => {
      if (!controller.signal.aborted && latestCatalogKey.current === key) setCatalog(next)
    }).catch((reason) => { if (!controller.signal.aborted) setCatalogError(errorText(reason)) })
    return () => controller.abort()
  }, [catalogRevision, requestId, tab])

  const controls = catalog ? <div className="simulation-dashboard__controls">
    <SelectField label="Simulation Case" value={caseId} choices={catalog.cases} onChange={(value) => { setCaseId(value); setCaptureId(''); setLoadCaseId(''); setRunId(''); setMode(''); setComponentId(''); setReferenceCaseId(''); setReferenceCaptureId('') }} />
    <SelectField label="Capture" value={captureId} choices={catalog.captures.filter((item) => item.case_id === caseId)} disabled={!caseId} onChange={(value) => { setCaptureId(value); setLoadCaseId(''); setRunId(''); setMode(''); setComponentId('') }} />
    {tab === 'distribution' ? <>
      <SelectField label="Load case" value={loadCaseId} choices={catalog.load_cases.filter((item) => item.case_id === caseId && item.capture_id === captureId)} disabled={!captureId} onChange={(value) => { setLoadCaseId(value); setRunId(''); setMode(''); setComponentId('') }} />
      <SelectField label="run #" value={runId} choices={catalog.execution_runs.filter((item) => item.case_id === caseId && item.load_case_id === loadCaseId && item.capture_id === captureId)} disabled={!loadCaseId} onChange={(value) => { setRunId(value); setMode(''); setComponentId('') }} />
      <SelectField label="Mode" value={mode} choices={catalog.modes.filter((item) => (item.execution_run_id === runId || item.run_id === runId) && item.capture_id === captureId)} disabled={!runId} onChange={(value) => { setMode(value); setComponentId('') }} />
      <SelectField label="Component" value={componentId} choices={catalog.components.filter((item) => (item.execution_run_id === runId || item.run_id === runId) && item.mode === mode && item.capture_id === captureId)} disabled={!mode} onChange={setComponentId} />
      <SelectField label="Basis" value={basis} choices={catalog.bases} onChange={(value) => setBasis(value === 'DETAIL' ? 'DETAIL' : value === 'REPORTED_SUMMARY' ? 'REPORTED_SUMMARY' : '')} />
    </> : <>
      <SelectField label="Reference Case" value={referenceCaseId} choices={catalog.cases.filter((item) => item.id !== caseId)} onChange={(value) => { setReferenceCaseId(value); setReferenceCaptureId('') }} />
      <SelectField label="Reference Capture" value={referenceCaptureId} choices={catalog.captures.filter((item) => item.case_id === referenceCaseId)} disabled={!referenceCaseId} onChange={setReferenceCaptureId} />
    </>}
  </div> : null

  return <section className="simulation-dashboard" aria-label="SPDM 해석 결과 대시보드">
    <header className="simulation-dashboard__head"><div><span>RESULT EXPLORER</span><h2>해석 결과 대시보드</h2><p>표시값과 자산은 선택한 Case·Run·capture 문맥의 서버 결과만 사용합니다.</p></div><nav aria-label="결과 환경"><button className={tab === 'usage' ? 'active' : ''} type="button" onClick={() => setTab('usage')}>사용환경</button><button className={tab === 'distribution' ? 'active' : ''} type="button" onClick={() => setTab('distribution')}>유통환경</button></nav></header>
    {catalogError ? <State message={catalogError} error /> : controls}
    <CaptureTools key={`${projectId}:${requestId}:${tab}`} projectId={projectId} requestId={requestId} environment={tab === 'usage' ? 'USAGE' : 'DISTRIBUTION'} onChanged={() => setCatalogRevision((value) => value + 1)} />
    {tab === 'usage' ? <UsageArea key={`${caseId}:${captureId}:${referenceCaseId}:${referenceCaptureId}`} caseId={caseId} captureId={captureId} referenceCaseId={referenceCaseId} referenceCaptureId={referenceCaptureId} /> : <DistributionArea key={`${projectId}:${requestId}`} projectId={projectId} requestId={requestId} caseId={caseId} loadCaseId={loadCaseId} captureId={captureId} runId={runId} mode={mode} componentId={componentId} basis={basis} />}
  </section>
}

function CaptureTools({ projectId, requestId, environment, onChanged }: { projectId: string; requestId: string; environment: 'USAGE' | 'DISTRIBUTION'; onChanged: () => void }) {
  const [relativePath, setRelativePath] = useState(''); const [storageRootId, setStorageRootId] = useState(''); const [candidates, setCandidates] = useState<Array<{ root_relative_path: string; source_name: string }>>([]); const [busy, setBusy] = useState(''); const [message, setMessage] = useState('')
  const operation = useRef<AbortController | null>(null); const latestContext = useRef('')
  useEffect(() => {
    operation.current?.abort(); latestContext.current = `${projectId}:${requestId}:${environment}`
    setRelativePath(''); setStorageRootId(''); setCandidates([]); setBusy(''); setMessage('')
    return () => operation.current?.abort()
  }, [environment, projectId, requestId])
  const isCurrent = (controller: AbortController, key: string) => !controller.signal.aborted && latestContext.current === key
  const scan = async () => {
    operation.current?.abort(); const controller = new AbortController(); operation.current = controller; const key = `${projectId}:${requestId}:${environment}`
    setBusy('scan'); setMessage('')
    try {
      const result = await simulationDashboardApi.scan({ project_id: projectId, request_id: requestId, root_relative_path: relativePath.trim(), environment }, controller.signal)
      if (!isCurrent(controller, key)) return
      setStorageRootId(result.storage_root_id); setCandidates(result.cases); setMessage(result.cases.length ? '조사 후보를 선택해 capture를 게시하세요.' : result.issues.join(' · ') || '발견된 Case가 없습니다.')
    } catch (reason) { if (isCurrent(controller, key)) setMessage(errorText(reason)) } finally { if (operation.current === controller) { operation.current = null; setBusy('') } }
  }
  const capture = async () => {
    if (!storageRootId || !relativePath) { setMessage('조사 결과에서 Case를 선택하세요.'); return }
    operation.current?.abort(); const controller = new AbortController(); operation.current = controller; const key = `${projectId}:${requestId}:${environment}`
    setBusy('capture'); setMessage('')
    try {
      await simulationDashboardApi.capture({ project_id: projectId, request_id: requestId, root_relative_path: relativePath, environment, storage_root_id: storageRootId }, controller.signal)
      if (!isCurrent(controller, key)) return
      setMessage('검증된 capture를 게시했습니다.'); onChanged()
    } catch (reason) { if (isCurrent(controller, key)) setMessage(errorText(reason)) } finally { if (operation.current === controller) { operation.current = null; setBusy('') } }
  }
  return <details className="simulation-dashboard__capture"><summary>원본 폴더 조사·capture 게시</summary><p>설정된 저장소 루트 아래의 상대 경로만 사용합니다. 빈 경로는 설정 루트 아래의 표준 Case를 읽기 전용으로 찾습니다.</p><div><label>상대 경로<input value={relativePath} onChange={(event) => setRelativePath(event.target.value)} placeholder="비워두면 설정 루트 전체 조사" /></label><button type="button" disabled={!!busy} onClick={() => void scan()}>{busy === 'scan' ? '조사 중…' : '읽기 전용 조사'}</button><button type="button" disabled={!!busy || !storageRootId || !relativePath} onClick={() => void capture()}>{busy === 'capture' ? '게시 중…' : '선택 Case capture 게시'}</button></div>{candidates.length ? <div className="simulation-dashboard__capture-candidates">{candidates.map((item) => <button type="button" key={item.root_relative_path} className={relativePath === item.root_relative_path ? 'active' : ''} onClick={() => setRelativePath(item.root_relative_path)}>{item.source_name} <small>{item.root_relative_path}</small></button>)}</div> : null}{message ? <small role="status">{message}</small> : null}</details>
}

function State({ message, error = false }: { message: string; error?: boolean }) { return <div className={`simulation-dashboard__state ${error ? 'error' : ''}`} role={error ? 'alert' : 'status'}>{error ? <AlertTriangle /> : <Layers3 />}<span>{message}</span></div> }

function UsageArea({ caseId, captureId, referenceCaseId, referenceCaptureId }: { caseId: string; captureId: string; referenceCaseId: string; referenceCaptureId: string }) {
  const [data, setData] = useState<UsageDashboard | null>(null)
  const [error, setError] = useState('')
  useEffect(() => {
    setData(null); setError('')
    if (!caseId || !captureId || Boolean(referenceCaseId) !== Boolean(referenceCaptureId)) return
    const controller = new AbortController()
    simulationDashboardApi.usage(caseId, captureId, referenceCaseId || undefined, referenceCaptureId || undefined, controller.signal)
      .then((next) => { if (!controller.signal.aborted) setData(next) })
      .catch((reason) => { if (!controller.signal.aborted) setError(errorText(reason)) })
    return () => controller.abort()
  }, [caseId, captureId, referenceCaseId, referenceCaptureId])
  if (!caseId || !captureId) return <State message="Simulation Case와 수집 버전을 선택하세요." />
  if (Boolean(referenceCaseId) !== Boolean(referenceCaptureId)) return <State message="Reference 수집 버전을 선택하세요." />
  if (error) return <State message={error} error />
  if (!data) return <State message="사용환경 평가를 불러오는 중입니다." />
  const media = data.evaluations.flatMap((evaluation) => evaluation.media ?? [])
  const cell = (value?: DashboardValue | null) => value ? <>{value.value == null ? (value.verdict ?? '값 없음') : valueText(value)}{value.value != null && value.verdict ? ` / ${value.verdict}` : ''}<small>{value.status !== 'READY' ? value.status : ''}</small></> : '해당 없음'
  return <div className="simulation-dashboard__usage" data-testid="usage-dashboard">
    <div className="simulation-dashboard__usage-media">{media.length ? media.map((asset) => <div key={asset.asset_id}><h3>{asset.title}</h3><Media asset={asset} /></div>) : <State message="연결된 미디어 없음" />}</div>
    <div className="simulation-dashboard__usage-table"><header><h3>다섯 평가 종합</h3><small>{data.status}</small></header><table><thead><tr><th>평가</th><th>공통</th><th>전방</th><th>후방</th><th>Reference</th></tr></thead><tbody>{data.evaluations.map((evaluation) => <tr key={evaluation.id}><th>{evaluation.name}</th><td>{cell(evaluation.common)}</td><td>{cell(evaluation.front)}</td><td>{cell(evaluation.rear)}</td><td>{evaluation.reference ? <>{evaluation.reference.reason || ['common', 'front', 'rear'].map((direction) => { const value = evaluation.reference?.[direction as 'common' | 'front' | 'rear']; return value ? <div key={direction}>{direction === 'common' ? '공통' : direction === 'front' ? '전방' : '후방'}: {cell(value)}</div> : null })}</> : '미선택'}</td></tr>)}</tbody></table></div>
    <div className="simulation-dashboard__usage-values">{data.evaluations.filter((evaluation) => evaluation.id !== 'Slope_Angle_360').map((evaluation) => {
      const points = (['common', 'front', 'rear'] as const).filter((key) => evaluation[key] != null).map((key) => ({ direction: key === 'common' ? '공통' : key === 'front' ? '전방' : '후방', current: evaluation[key]?.value, reference: evaluation.reference?.[key]?.value }))
      return <article key={evaluation.id}><h3>{evaluation.name}</h3><small>{evaluation.common?.unit || evaluation.front?.unit || evaluation.rear?.unit}</small><div style={{height: 150}}><ResponsiveContainer width="100%" height="100%"><BarChart data={points}><XAxis dataKey="direction" /><YAxis /><Tooltip /><Bar dataKey="current" name="현재 Case" fill="var(--color-chart-series-1)" /><Bar dataKey="reference" name="Reference" fill="var(--color-chart-series-2)" /></BarChart></ResponsiveContainer></div></article>
    })}</div>{data.quality_issues.length ? <Issues issues={data.quality_issues} /> : null}
  </div>
}

function DistributionArea({ projectId, requestId, caseId, loadCaseId, captureId, runId, mode, componentId, basis }: { projectId: string; requestId: string; caseId: string; loadCaseId: string; captureId: string; runId: string; mode: string; componentId: string; basis: '' | 'REPORTED_SUMMARY' | 'DETAIL' }) {
  const [edges, setEdges] = useState<string[]>([...EDGES]); const [lines, setLines] = useState<string[]>(['1', '2', '3', '4']); const [data, setData] = useState<DashboardDistribution | null>(null); const [error, setError] = useState(''); const [sceneId, setSceneId] = useState(''); const [memberId, setMemberId] = useState(''); const [section, setSection] = useState<'summary' | 'edges' | 'contours' | 'behavior'>('summary'); const [comparison, setComparison] = useState<DashboardComparisonMember[]>([]); const latestKey = useRef('')
  useEffect(() => { setComparison([]); setData(null); setSceneId(''); setMemberId('') }, [projectId, requestId])
  const contextKey = `${projectId}:${requestId}:${caseId}:${loadCaseId}:${runId}:${mode}:${captureId}:${componentId}:${basis}:${edges.join(',')}:${lines.join(',')}:${comparison.map((item) => `${item.simulation_case_id}/${item.load_case_id}/${item.execution_run_id}/${item.capture_id}/${item.mode}/${item.component_id}/${item.basis}`).join('|')}`
  useEffect(() => {
    if (!runId || !captureId || !mode || !componentId || !basis) { setData(null); return }
    const controller = new AbortController(); latestKey.current = contextKey; setError(''); setData(null); setSceneId(''); setMemberId('')
    const current = { simulation_case_id: caseId, load_case_id: loadCaseId, execution_run_id: runId, capture_id: captureId, mode, component_id: componentId, basis }
    const members = comparison.length ? [...comparison, ...(comparison.some((item) => item.simulation_case_id === caseId) ? [] : [current])] : []
    const request = members.length > 1 ? simulationDashboardApi.comparison(members, edges.join(','), lines.join(','), controller.signal) : simulationDashboardApi.distribution(runId, { capture_id: captureId, mode, component_id: componentId, basis, edge_keys: edges.join(','), line_indices: lines.join(',') }, controller.signal)
    request.then((next) => { if (!controller.signal.aborted && latestKey.current === contextKey) setData(next) }).catch((reason) => { if (!controller.signal.aborted && latestKey.current === contextKey) setError(errorText(reason)) })
    return () => controller.abort()
  }, [basis, captureId, caseId, comparison, componentId, contextKey, edges, lines, loadCaseId, mode, projectId, requestId, runId])
  if (!runId || !captureId || !mode || !componentId || !basis) return <State message="Run, mode, component, basis와 capture를 모두 선택하세요." />
  if (error) return <State message={error} error />
  if (!data) return <State message="유통환경 요약을 불러오는 중입니다." />
  const choose = (scene: string, member: string) => { setSceneId(scene); setMemberId(member) }
  const addCurrent = () => { const next = { simulation_case_id: caseId, load_case_id: loadCaseId, execution_run_id: runId, capture_id: captureId, mode, component_id: componentId, basis: basis as DashboardComparisonMember['basis'] }; if (!comparison.some((item) => item.simulation_case_id === caseId) && comparison.length < 8) setComparison([...comparison, next]) }
  return <div className="simulation-dashboard__distribution" data-testid="distribution-dashboard"><div className="simulation-dashboard__toolbar"><EdgePicker edges={edges} onChange={setEdges} /><LinePicker lines={lines} onChange={setLines} /><button type="button" onClick={addCurrent} disabled={comparison.some((item) => item.simulation_case_id === caseId) || comparison.length >= 8}>현재 Case 비교에 추가</button>{comparison.map((item) => <button type="button" key={item.simulation_case_id} onClick={() => setComparison(comparison.filter((candidate) => candidate.simulation_case_id !== item.simulation_case_id))}>Case 제거 {item.simulation_case_id}</button>)}<nav aria-label="유통환경 상세"><button className={section === 'summary' ? 'active' : ''} onClick={() => setSection('summary')}>결과 요약</button><button className={section === 'edges' ? 'active' : ''} onClick={() => setSection('edges')}>엣지별 수준</button><button className={section === 'contours' ? 'active' : ''} onClick={() => setSection('contours')}>컨투어</button><button className={section === 'behavior' ? 'active' : ''} onClick={() => setSection('behavior')}>거동</button></nav></div>{section === 'summary' ? <Summary data={data} sceneId={sceneId} memberId={memberId} selectedEdges={edges} onSelect={choose} /> : null}{section === 'edges' ? <EdgePanels data={data} onSelect={choose} /> : null}{section === 'contours' ? <ContourMatrix data={data} onSelect={choose} /> : null}{section === 'behavior' ? <BehaviorMatrix data={data} sceneId={sceneId} onSelect={choose} /> : null}<SimulationLocationMap data={data} onSelect={choose} /><SceneDetail sceneId={sceneId} member={data.members.find((member) => member.id === memberId)} lineIndices={lines.join(',')} />{data.quality_issues.length ? <Issues issues={data.quality_issues} /> : null}</div>
}

function EdgePicker({ edges, onChange }: { edges: string[]; onChange: (next: string[]) => void }) { const toggle = (edge: string) => onChange(edges.includes(edge) ? edges.filter((item) => item !== edge) : [...edges, edge]); return <fieldset className="simulation-dashboard__picker"><legend>표시 엣지</legend>{EDGES.map((edge) => <label key={edge}><input type="checkbox" checked={edges.includes(edge)} onChange={() => toggle(edge)} />{edge}</label>)}</fieldset> }
function LinePicker({ lines, onChange }: { lines: string[]; onChange: (next: string[]) => void }) { const toggle = (line: string) => onChange(lines.includes(line) ? lines.filter((item) => item !== line) : [...lines, line]); return <fieldset className="simulation-dashboard__picker"><legend>네 라인</legend>{['1', '2', '3', '4'].map((line) => <label key={line}><input type="checkbox" checked={lines.includes(line)} onChange={() => toggle(line)} />L{line}</label>)}</fieldset> }
function memberColor(member: DashboardMember, index: number) { return member.color || COLORS[index % COLORS.length] }
function sceneLabel(scene: DashboardScene) { return scene.scene_sequence_number == null ? scene.label : String(scene.scene_sequence_number) }
function Summary({ data, sceneId, memberId, selectedEdges, onSelect }: { data: DashboardDistribution; sceneId: string; memberId: string; selectedEdges: string[]; onSelect: (sceneId: string, memberId: string) => void }) { const selectedMember = data.members.find((member) => member.id === memberId) ?? data.members[0]; const selectedScene = data.scenes.find((scene) => scene.id === sceneId) ?? data.scenes[0]; const peaks = selectedMember && selectedScene ? data.edge_peaks.filter((peak) => peak.member_id === selectedMember.id && peak.scene_id === selectedScene.id) : []; const rows = data.scenes.flatMap((scene) => data.members.map((member, index) => { const point = data.series.find((item) => item.scene_id === scene.id && item.member_id === member.id); return { scene_id: scene.id, member_id: member.id, scene: sceneLabel(scene), member: member.label, color: memberColor(member, index), value: point?.selected_edge_envelope ?? null, unit: point?.unit ?? null } })); return <div className="simulation-dashboard__summary"><section><header><h3>Open Cell 엣지 맵</h3><small>{selectedScene?.label ?? 'Scene 선택'}</small></header><EdgeMap peaks={peaks} selectedEdges={selectedEdges} /></section><section><header><h3>Scene별 엣지 최대응력</h3><small>선택 엣지 envelope는 서버가 반환한 값입니다.</small></header><div className="simulation-dashboard__chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={rows} onClick={(state) => { const point = state?.activePayload?.[0]?.payload as typeof rows[number] | undefined; if (point) onSelect(point.scene_id, point.member_id) }}><CartesianGrid vertical={false} strokeDasharray="3 3" /><XAxis dataKey="scene" /><YAxis /><Tooltip formatter={(value: number, _name, item) => [`${value} ${item?.payload?.unit ?? '단위 미확인'}`, '선택 엣지 envelope']} /><Bar dataKey="value">{rows.map((row) => <Cell key={`${row.scene_id}:${row.member_id}`} fill={row.color} />)}</Bar></BarChart></ResponsiveContainer></div></section></div> }
function EdgeMap({ peaks, selectedEdges }: { peaks: DashboardEdgePeak[]; selectedEdges: string[] }) { const item = (edge: string) => peaks.find((peak) => peak.edge === edge); return <div className="simulation-dashboard__edge-map">{EDGES.map((edge) => { const peak = item(edge); return <div key={edge} className={`edge edge-${edge.toLowerCase()} ${selectedEdges.includes(edge) ? 'selected' : ''} ${peak?.is_selected_maximum ? 'maximum' : ''}`}><b>{edge}</b><span>{valueText(peak)}</span><small>{peak?.completeness ?? peak?.status ?? '자료 없음'}</small></div> })}</div> }
function EdgePanels({ data, onSelect }: { data: DashboardDistribution; onSelect: (sceneId: string, memberId: string) => void }) { const [visible, setVisible] = useState<string[]>([]); const [order, setOrder] = useState<'scene' | 'case'>('scene'); const [size, setSize] = useState<'compact' | 'wide'>('wide'); const members = visible.length ? data.members.filter((member) => visible.includes(member.id)) : data.members; const rows = (edge: string) => { const source = order === 'scene' ? data.scenes.flatMap((scene) => members.map((member, index) => ({ scene, member, index }))) : members.flatMap((member, index) => data.scenes.map((scene) => ({ scene, member, index }))); return source.map(({ scene, member, index }) => { const point = data.edge_peaks.find((item) => item.edge === edge && item.scene_id === scene.id && item.member_id === member.id); return { scene_id: scene.id, member_id: member.id, scene: sceneLabel(scene), member: member.label, value: point?.value ?? null, unit: point?.unit ?? null, color: memberColor(member, index) } }) }; return <><SceneTable scenes={data.scenes} /><div className="simulation-dashboard__panel-controls"><fieldset><legend>Case 범례</legend>{data.members.map((member, index) => <label key={member.id}><input type="checkbox" checked={!visible.length || visible.includes(member.id)} onChange={() => setVisible((current) => current.length ? current.includes(member.id) ? current.filter((id) => id !== member.id) : [...current, member.id] : data.members.filter((item) => item.id !== member.id).map((item) => item.id))} /><i style={{ background: memberColor(member, index) }} />{member.label}</label>)}</fieldset><label>표시 순서<select value={order} onChange={(event) => setOrder(event.target.value as 'scene' | 'case')}><option value="scene">Scene별</option><option value="case">Case별</option></select></label><label>패널 크기<select value={size} onChange={(event) => setSize(event.target.value as 'compact' | 'wide')}><option value="compact">작게</option><option value="wide">크게</option></select></label></div><div className={`simulation-dashboard__edge-panels ${size}`}>{EDGES.map((edge) => { const points = rows(edge); return <section key={edge}><header><h3>{edge}</h3><small>서버가 제공한 엣지별 최대값</small></header><div className="simulation-dashboard__chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={points} onClick={(state) => { const point = state?.activePayload?.[0]?.payload as typeof points[number] | undefined; if (point) onSelect(point.scene_id, point.member_id) }}><CartesianGrid vertical={false} strokeDasharray="3 3" /><XAxis dataKey="scene" /><YAxis /><Tooltip formatter={(value: number, _name, item) => [`${value} ${item?.payload?.unit ?? '단위 미확인'}`, edge]} /><Bar dataKey="value">{points.map((point) => <Cell key={`${point.scene_id}:${point.member_id}`} fill={point.color} />)}</Bar></BarChart></ResponsiveContainer></div></section> })}</div></> }
function SceneTable({ scenes }: { scenes: DashboardScene[] }) { return <div className="simulation-dashboard__scene-table"><span>Scene 설명</span>{scenes.map((scene) => <div key={scene.id}><b>{scene.scene_sequence_number ?? '순번 미확인'}</b><span>{scene.label}</span><small>{[scene.contact_code, scene.repetition, scene.order_status].filter(Boolean).join(' · ') || '설명 미확인'}</small></div>)}</div> }
function ContourMatrix({ data, onSelect }: { data: DashboardDistribution; onSelect: (sceneId: string, memberId: string) => void }) { const [transpose, setTranspose] = useState(false); const rows = transpose ? data.members : data.scenes; const columns = transpose ? data.scenes : data.members; return <section className="simulation-dashboard__matrix"><header><div><h3>Scene별 Cell Tmax 마지막 Frame Contour</h3><small>FINAL_FRAME 근거가 없는 자산은 프레임 미확인으로 표시합니다.</small></div><button type="button" onClick={() => setTranspose(!transpose)}><RotateCcw /> 행/열 전치</button></header><div className="simulation-dashboard__matrix-scroll"><div className="simulation-dashboard__matrix-grid" style={{ gridTemplateColumns: `minmax(180px, .7fr) repeat(${columns.length}, minmax(190px, 1fr))` }}><strong>{transpose ? 'Case' : 'Scene'}</strong>{columns.map((column) => <strong key={column.id}>{column.label}</strong>)}{rows.flatMap((row) => [<strong key={`${row.id}-label`}>{'scene_sequence_number' in row ? `${row.scene_sequence_number ?? '순번 미확인'} · ${row.label}` : row.label}</strong>, ...columns.map((column) => { const scene = transpose ? column as DashboardScene : row as DashboardScene; const member = transpose ? row as DashboardMember : column as DashboardMember; const cell = data.contours.find((candidate) => candidate.scene_id === scene.id && candidate.member_id === member.id); return <MatrixCell key={cell?.cell_id ?? `${scene.id}:${member.id}`} cellId={cell?.cell_id ?? `${scene.id}:${member.id}`} asset={cell?.asset} status={cell?.status} reason={cell?.reason} onClick={() => onSelect(scene.id, member.id)} /> })])}</div></div></section> }
function BehaviorMatrix({ data, sceneId, onSelect }: { data: DashboardDistribution; sceneId: string; onSelect: (sceneId: string, memberId: string) => void }) { const scene = data.scenes.find((item) => item.id === sceneId) ?? data.scenes[0]; if (!scene) return <State message="Scene이 없습니다." />; return <section className="simulation-dashboard__matrix"><header><div><h3>Scene별 거동</h3><small>{scene.label}</small></div></header><div className="simulation-dashboard__matrix-scroll"><div className="simulation-dashboard__matrix-grid" style={{ gridTemplateColumns: `minmax(150px,.45fr) repeat(${data.members.length}, minmax(190px, 1fr))` }}><strong>대상</strong>{data.members.map((member) => <strong key={member.id}>{member.label}</strong>)}{ROLES.flatMap((role) => [<strong key={`${role}-label`}>{role}</strong>, ...data.members.map((member) => { const cell = data.behaviors.find((candidate) => candidate.scene_id === scene.id && candidate.member_id === member.id && candidate.subject_role === role); return <MatrixCell key={cell?.cell_id ?? `${scene.id}:${member.id}:${role}`} cellId={cell?.cell_id ?? `${scene.id}:${member.id}:${role}`} asset={cell?.asset} status={cell?.status} reason={cell?.reason} onClick={() => onSelect(scene.id, member.id)} /> })])}</div></div></section> }
function MatrixCell({ cellId, asset, status, reason, onClick }: { cellId: string; asset?: DashboardAsset | null; status?: string; reason?: string | null; onClick: () => void }) { return <div className="simulation-dashboard__matrix-cell" role="button" tabIndex={0} data-cell-id={cellId} onClick={onClick} onKeyDown={(event) => { if (event.key === 'Enter' || event.key === ' ') onClick() }}>{asset ? <Media asset={asset} /> : <span>{missingText(status, reason)}</span>}</div> }
function SceneDetail({ sceneId, member, lineIndices }: { sceneId: string; member?: DashboardMember; lineIndices: string }) { const [data, setData] = useState<DashboardSceneDetail | null>(null); const [error, setError] = useState(''); const [position, setPosition] = useState('TOP'); const latestKey = useRef('')
  useEffect(() => { if (!sceneId || !member) { setData(null); return }; const controller = new AbortController(); const key = `${sceneId}:${member.id}:${lineIndices}:${position}`; latestKey.current = key; setData(null); setError(''); simulationDashboardApi.sceneDetail(sceneId, { execution_run_id: member.execution_run_id, capture_id: member.capture_id, mode: member.mode, component_id: member.component_id, basis: member.basis, line_indices: lineIndices, position }, controller.signal).then((next) => { if (!controller.signal.aborted && latestKey.current === key) setData(next) }).catch((reason) => { if (!controller.signal.aborted && latestKey.current === key) setError(errorText(reason)) }); return () => controller.abort() }, [lineIndices, member, position, sceneId])
  if (!sceneId || !member) return null
  if (error) return <State message={error} error />
  if (!data) return <State message="선택 Scene 상세를 불러오는 중입니다." />
  return <section className="simulation-dashboard__detail"><header><h3>Scene 상세 · {data.scene.label}</h3><label>위치 <select value={position} onChange={(event) => setPosition(event.target.value)} aria-label="Scene 상세 위치">{['TOP', 'BOT', 'LH', 'RH'].map((item) => <option key={item}>{item}</option>)}</select></label><small>{data.context.context_key}</small></header><div className="simulation-dashboard__chart"><ResponsiveContainer width="100%" height="100%"><LineChart><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="ref_coord" type="number" /><YAxis /><Tooltip /><Legend />{[1, 2, 3, 4].map((line) => <Line key={line} data={data.line_points.filter((point) => point.line_index === line)} dataKey="value" name={`L${line}`} type="linear" connectNulls={false} stroke={COLORS[line - 1]} dot={false} />)}</LineChart></ResponsiveContainer></div><div className="simulation-dashboard__detail-assets">{data.assets.map((asset) => <Media key={asset.asset_id} asset={asset} />)}</div>{data.quality_issues.length ? <Issues issues={data.quality_issues} /> : null}</section> }
function Media({ asset }: { asset: DashboardAsset }) { const [expanded, setExpanded] = useState(false); const [playError, setPlayError] = useState(''); const video = useRef<HTMLVideoElement>(null); const dialog = useRef<HTMLDialogElement>(null); const close = () => { dialog.current?.close(); setExpanded(false) }; useEffect(() => { setExpanded(false); setPlayError(''); video.current?.pause() }, [asset.asset_id]); useEffect(() => { if (expanded && dialog.current && !dialog.current.open) dialog.current.showModal() }, [expanded]); const toggle = async () => { if (!video.current) return; try { if (video.current.paused) await video.current.play(); else video.current.pause(); setPlayError('') } catch { setPlayError('영상을 재생하지 못했습니다.') } }; const frame = asset.frame_role === 'FINAL_FRAME' ? '최종 프레임' : '프레임 미확인'; return <div className="simulation-dashboard__media">{asset.kind === 'VIDEO' ? <video ref={video} controls src={assetUrl(asset)} aria-label={asset.title ?? '결과 영상'} /> : asset.status === 'READY' ? <img src={assetUrl(asset)} alt={asset.title ?? '결과 이미지'} /> : <span><ImageIcon />{asset.status}</span>}<small>{frame}{asset.provenance ? ` · ${asset.provenance}` : ''}</small><div className="simulation-dashboard__media-actions">{asset.kind === 'VIDEO' ? <button type="button" aria-label="영상 재생 또는 일시정지" onClick={() => void toggle()}><Play /></button> : null}<button type="button" aria-label="자산 확대" onClick={() => setExpanded(true)}><Expand /></button></div>{playError ? <em role="alert">{playError}</em> : null}{expanded ? <dialog ref={dialog} className="simulation-dashboard__lightbox" onCancel={(event) => { event.preventDefault(); close() }} onClose={() => setExpanded(false)}><button type="button" onClick={close} aria-label="확대 보기 닫기">닫기</button>{asset.kind === 'VIDEO' ? <video controls autoPlay src={assetUrl(asset)} aria-label={asset.title ?? '확대 결과 영상'} /> : <img src={assetUrl(asset)} alt={asset.title ?? '결과 이미지 확대'} />}</dialog> : null}</div> }
function Issues({ issues }: { issues: string[] }) { return <aside className="simulation-dashboard__issues"><AlertTriangle />{issues.map((issue) => <span key={issue}>{issue}</span>)}</aside> }
