# 개발 및 배포 지침

## 협업과 토큰 효율

- 에이전트 역할·위임·추론 강도·컨텍스트 관리는 최신 Codex 사용 지침을 따른다. 이 파일은 프로젝트 고유 규칙과 필수 배포 계약을 보완한다.
- 저위험 소규모 수정은 주 에이전트가 직접 처리한다. 일반 기능 개발은 Astra 지휘·설계 → 기능별 Luna/Terra 구현 → 독립 Sol 1차 검수 → 지휘 Astra 최종 통합 검수로 진행한다.
- 대규모 구조 변경과 인증·권한·데이터 보존·DB migration·배포 동작 변경에는 별도의 독립 Astra 최종 검수자를 배정한다. 문구 정리는 실제 계약 변경 여부로 판단한다.
- 기능별 담당 파일과 인터페이스를 정하고 공통 변경은 협의한다. 같은 파일의 동시 수정과 중복 조사를 피하며, 위임에는 목표·관련 파일·유지할 계약·필수 검증만 간결하게 전달한다.

## 프로젝트 작업 원칙

- 최신 사용자 요청과 명시된 단계별 확인 조건을 지킨다. 기존 미커밋 변경을 보존한다.
- UI 작업은 데스크톱 전용 기준으로 계획·구현·검수한다(2026-09-20 사용자 영구 지침). 모바일 전용 UI 개선·최적화·모바일 뷰포트 검수·신규 모바일 테스트를 작업 범위와 완료 조건에 넣지 않는다. 기존 반응형 코드의 일괄 제거를 뜻하지 않는다. 사용자가 명시적으로 변경하기 전까지 유지하며, 과거 모바일 요구와 충돌하면 이 지침 및 `docs/dashboard/ui-density-improvement-plan.md`의 적용 대상 정책을 우선한다.
- 실제 사용자 DB·설정·실행 중 서비스를 테스트 대상으로 사용하지 않는다. 격리된 임시 환경과 합성 데이터로 검증한다.
- 기능별 독립 구조와 명확한 인터페이스를 유지한다. 검증된 오픈소스·패키지를 적극 재사용하되 기존 의존성, 라이선스, 유지보수 상태, Windows·오프라인 배포 적합성을 확인한다.
- 문서 지도 `docs/README.md`에서 관련 기능 문서를 찾아 필요한 부분만 읽는다. 검색으로 범위를 좁히고 큰 문서·로그·산출물의 전체 읽기와 반복 전달을 피한다.
- 중요한 변경·결정·검증 결과는 `log/work-log.md`에 간결하게 기록한다. 큰 변경은 최신 사용자 요청에 맞춰 관련 개발문서를 함께 갱신하고 새 문서는 문서 지도에 연결한다.
- 검수는 diff·요구사항·관련 계약·검증 결과부터 확인한다. 변경에 맞는 검사와 필수 CI를 수행하고, 새 변경·실패·위험 없이 통과한 검사를 반복하지 않는다. 저위험 문서 수정은 내용·링크·diff를 확인한다.
- 완료 보고는 결과·검증·남은 문제를 간결하게 밝힌다. 미실행·실패·환경 제약을 통과와 구분하며, 모델 미가용 등으로 수행하지 못한 독립 검수를 완료로 기록하지 않는다.

## Codex Security 활용

- Codex Security는 개발 단계의 보안 검수 도구로 활용하며, 배포 프로그램의 런타임 의존성에 포함하지 않는다.
- 인증·권한, 비밀정보 처리, 외부 입력·파일 업로드·경로 접근, SQL·명령 실행, 네트워크 노출, 배포 권한 등 보안 경계를 변경할 때 변경분 보안 검수(`security-diff-scan`)를 수행한다. 단순 문구·색상·배치 수정 등 보안 영향이 없는 작업에는 일괄 적용하지 않는다.
- 일반 변경은 기준 커밋 또는 패치를 확정하고 변경분과 관련 실행 경로를 검수한다. 전체 저장소 검수(`security-scan`)는 초기 보안 기준 수립이나 명시적인 전체 점검 요청에 사용하며, 반복·심층 검수(`deep-security-scan`)는 사용자가 요청한 경우에 사용한다. 선택한 스캔의 필수 단계와 범위는 생략하지 않는다.
- Sol 1차 검수에서 보안 검수가 필요한 범위를 정하고 결과를 활용한다. 일반 회귀·요구사항 검수와 보안 검수의 역할을 구분하고, 동일 변경·동일 근거의 스캔은 반복하지 않는다. Astra의 통합 검수와 고위험 변경의 독립 Astra 검수는 유지한다.
- 발견 사항은 추정과 검증된 취약점을 구분하고, 코드 위치·도달 조건·영향·검증 근거를 기록한다. 수정 후 관련 보안 경로와 회귀를 확인하되, 별도 `verify-fix` 워크플로는 사용자가 명시적으로 요청한 경우에 실행한다.
- 실제 사용자 DB·설정·실행 중 서비스에 공격성 검증을 수행하지 않는다. 재현은 격리된 임시 환경과 합성 데이터로 수행하고, 보고서에 비밀정보를 포함하지 않는다.
- 보안 검수는 기능 정확성·데이터 보존·DB migration·폐쇄망 배포 검증을 대체하지 않는다. 도구 미가용·실패·미검수 범위는 명시하고, 대체 수동 검수를 플러그인 스캔 완료로 기록하지 않는다.
- 중요한 발견·조치·검증 결과와 남은 위험은 `log/work-log.md`에 간결히 기록한다. 보안 점검 요청만으로 범위 밖 코드 수정·외부 이슈 등록·게시·배포까지 승인된 것으로 해석하지 않는다.

## 모든 개발에서 유지할 배포 계약

배포의 현재 기준은 [`docs/windows-deployment-policy.md`](docs/windows-deployment-policy.md)다. 기능·DB 스키마·의존성·실행 경로를 바꾸어도 이 정책과 사용자 진입점을 유지한다. 과거 문서에 Windows 운영 배포가 제외되어 있더라도 ADR 0005와 이 정책을 우선한다.

1. 소스 최초 설치는 `deploy.bat`, 소스 업데이트는 `update.bat`을 유지한다. 최초 설치에서 PostgreSQL 외의 Python·Node.js·Git·패키지 관리자의 사전 설치를 요구하지 않는다. VS Code는 필수가 아니다.
2. 폐쇄망 서버는 Windows Server 2022 x64만 있다고 가정한다. 실행 파일·Python wheel·웹 빌드·DB 도구·서비스 래퍼·필수 런타임을 외부에서 패키징한다. 대상 설치·업데이트·서비스 시작은 인터넷 다운로드, Git, Node.js, VS Code를 요구하면 안 된다.
3. DB 없음/실제 빈 DB/기존 앱 DB/알 수 없는 DB/접속 오류를 구분한다. 일반 배포에서 DB 삭제, `--replace-existing`, 기존 데이터 seed·reset, 무조건 Alembic stamp를 추가하지 않는다. 기존 DB는 검증한 백업 후 대기 중인 migration만 적용한다.
4. `.env`, owner 연결, 인증 키, 계정, DB 데이터, 관리형 첨부파일과 백업을 보존한다. 사용자 상태를 배포 패키지에 포함하지 않는다. 앱 실행은 스키마 검사만 하고 스키마 변경은 배포 단계에서만 한다.
5. 신규 의존성은 lock 파일, Windows x64 지원, 오프라인 wheel/바이너리 포함·검증 경로를 함께 갱신한다. 새 migration은 기존 데이터가 있는 업데이트와 빈 설치 모두를 검증한다. 서비스의 영구 경로를 릴리스 교체 경로 안으로 옮기지 않는다.
6. 배포 관련 변경은 Windows 배포 계약 CI와 관련 실패 경로 검사를 통과해야 한다. 실제 Server 2022의 폐쇄망 신규 설치·기존 DB 업데이트·재부팅 검증을 수행하지 않았다면 수행한 것으로 기록하지 않는다.
7. 배포 정책을 바꾸어야 하는 기능은 기능 편의를 위해 묵시적으로 예외 처리하지 않는다. 사용자 지시를 확인하고 ADR, 정책, 사용자 안내, 검증을 같은 변경에서 갱신한다. 기존 정책 안의 구현·수정에는 추가 승인을 요구하지 않는다.
