# 해석 데이터베이스 기반 사용자 구성형 시각화 대시보드 MVP

## 목표

Altair Panopticon과 같은 분석 대시보드의 사용 경험을 참고하되 특정 제품을 복제하지 않는다. 사용자는 준비된 데이터 변수와 시각화 위젯을 선택하고, 반응형 그리드에서 위치와 크기를 조절하고, 이름과 설명을 붙인 레이아웃을 저장·불러오기·복제·복구할 수 있어야 한다.

이번 Goal의 최우선 범위는 PostgreSQL 인프라가 아니라 완성도 높은 한국어 웹 대시보드, 대시보드 빌더, 재현 가능한 예제 데이터와 상호작용이다. 현재는 파일 기반 DuckDB를 사용하되 데이터 접근 계층·저장소 인터페이스·도메인 모델을 UI에서 분리해 향후 PostgreSQL로 이전할 수 있게 한다. 외부 배포와 실제 PostgreSQL 연결은 제외한다.

## 기술 기준

- 프런트엔드: React, TypeScript, Vite
- 대시보드 UI: 반응형 그리드 기반 드래그앤드롭 및 크기 조절
- 차트: 기존 Recharts를 보존하고 필요한 경우 확장
- 백엔드: Python, FastAPI
- 현재 DB: DuckDB
- 향후 DB: PostgreSQL
- 테스트: 프런트엔드 단위 테스트, 백엔드 pytest, 핵심 사용자 흐름과 실제 화면 검증
- 기존 사용자 파일과 변경을 보존하고 전면 재작성하지 않는다.

## 핵심 사용자 경험

1. 프로젝트 또는 해석 의뢰를 선택한다.
2. 원시 테이블 대신 시스템에 등록된 변수 카탈로그를 탐색한다.
3. 차트, 그래프, 표, 다이어그램, KPI, 상태 위젯을 추가한다.
4. 변수, 집계, 필터, 기준값, 색상과 표현 방식을 설정한다.
5. 위젯을 이동하고 크기를 조절한다.
6. 레이아웃을 이름과 설명을 붙여 저장한다.
7. 저장본을 불러오기·다른 이름으로 저장·복제·수정하고 최근 정상 버전으로 복구한다.
8. 자연어로 시각화 추가 또는 개선 변경안을 만든다.
9. 구조화되고 검증된 변경안을 미리 본 뒤 적용하거나 취소한다.
10. 전체 필터와 위젯 필터가 실제 데이터 쿼리에 연동된다.

조회 모드와 편집 모드는 명확히 구분한다. 복잡한 편집은 데스크톱을 기본으로 하되 모바일에서도 조회와 간단한 조작이 가능해야 한다.

## MVP 위젯 카탈로그

- KPI/단일 값, 패스·실패 판정, 임계값 게이지
- 선/복수 시계열, 막대/누적 막대/원형/분포, 산점도
- 데이터 테이블, 상태 배지
- 작업 진행 단계/타임라인, 작업 흐름 다이어그램
- 이미지/컨투어 뷰어, 영상 플레이어, 경량 3차원 결과 뷰어
- 텍스트 판정과 수행자 의견 패널

각 위젯은 ID·종류·제목·설명, 변수 바인딩, 프로젝트/의뢰/하중 경우 필터, 집계/변환, 기준값/판정, 색상/표시 형식, 그리드 위치/크기, 새로고침/빈 데이터 처리, 생성·수정 이력을 가진다.

## 변수 카탈로그

각 변수는 ID와 표시 이름, 프로젝트와 해석 유형, 데이터 유형(실수·정수·텍스트·판정·시계열·이미지·영상·3차원), 단위, 설명, 허용 집계와 차트, 기준값/정상 범위, 출처, 시간축/분류축, 필터 가능 여부, 향후 민감도·표시 권한 필드를 가진다. 사용자는 허용된 변수와 위젯 조합만 선택할 수 있다.

## 자연어 편집 안전 모델

자연어는 SQL·셸·코드를 직접 실행하지 않는다. `요청 → 의도/변수 식별 → 구조화 변경 명세 → 스키마/허용 변수 검증 → 미리보기 → 적용/취소 → 새 레이아웃 버전 저장` 순서로 처리한다. 외부 AI API 없이 규칙 기반 예제를 지원하며, AI 연동부는 별도 어댑터로 둔다. 모호하거나 없는 변수는 실행하지 않고 선택지를 제시한다.

## 레이아웃 저장

JSON 기반 정의를 DuckDB에서 관리한다. 대시보드 ID·이름·설명·대상·유형, 전체 필터, 위젯·바인딩·설정·위치·크기, 버전, 생성자/수정자와 시간, 마지막 정상 버전을 저장한다. MVP는 저장·불러오기·복제·최근 정상 버전 복구를 구현한다.

## PPT 보고서 레이아웃

보고서 내보내기는 16:9 슬라이드를 직접 보면서 제목·텍스트·판정·결과 카드·차트·표·이미지 위젯을 이동하고 크기를 조절하는 시각 편집기를 제공한다. 변수 카탈로그 변수를 끌어 놓아 데이터에 연결하며, 슬라이드 추가·복제·순서·반복 규칙과 레이아웃 버전을 저장한다.

회사 PowerPoint 양식은 규칙 기반 `.pptx` 템플릿으로 업로드한다. 도형 이름 또는 `{{variable:key}}`, `{{field:key}}` 태그를 검사하고 변수·보고서 필드와 연결한 뒤 원본 OOXML 디자인을 유지해 출력한다. 임의 PPTX 전체 편집, 매크로, OLE, 애니메이션 재작성은 범위에서 제외한다.

## 해석 데이터 도메인

```text
프로젝트
├─ 제품정보 ─ CAD / 재료 / 신뢰성
├─ 해석 의뢰작업 ─ 작업 순서·상태 / 담당자 / 시간 / 정성적 의견
├─ 하중 경우 ─ 낙하 / Side Clamp / 확장 유형
│  ├─ 모델링 자동화 템플릿 ─ 버전 / 입력 / 실행 이력
│  └─ 해석 결과 ─ 실수 / 정수 / 텍스트 / 시계열 / 컨투어 / 영상 / 경량 3D
└─ Validation ─ 신뢰성 판정 / 센서·측정 / AI 분석
```

공통 결과 메타데이터와 유형별 저장 구조를 분리한다. 이미지·영상·CAD·3D는 DB에 바이너리로 넣지 않고 파일 경로, 형식, 크기, 체크섬, 메타데이터를 저장한다. 웹 3D는 glTF/GLB만 허용하고 파일 경로·형식·크기를 검증한다.

## 필수 예제 1: TV 포장 낙하

Open Cell 상·하·좌·우 엣지 최대 응력, 엣지별 응력-시간, 기준값, 엣지/전체 판정, 낙하 방향·높이·하중 조건, 컨투어 이미지, 수행자 의견을 재현 가능한 샘플로 제공한다. 화면은 전체 판정 KPI, 기준 초과 색상 막대, 기준선 포함 시계열, 엣지 필터, 최대 시간, 상세 표, 컨투어/의견과 저장된 기본 레이아웃을 포함한다. 한 엣지라도 기준을 초과하면 기본적으로 FAIL이며 규칙을 설정에서 바꿀 수 있다. Side Clamp에서도 Open Cell 파손 분석을 지원한다.

Chassis Rear는 영구변형만 평가한다. 상·하 엣지는 변형된 양 끝 꼭지점 직선에서 가장 멀어진 거리와 위치를 계산하고, 모서리 변형도 평가한다. 기본 기준은 5 mm이며 관리자가 변경할 수 있다.

## 필수 예제 2: 해석 의뢰 작업 상태

여러 의뢰가 가로 방향으로 동시에 보이고 프로젝트/의뢰/제품/작업자/시간 기준으로 분류·정렬된다. 기본 단계는 `의뢰 접수 → 요구사항 검토 → 모델 준비 → 해석 전처리 모델링 → 해석 실행 → 후처리 작업 → 결과 검토 → Validation(선택) → 승인 → 완료`이다. 이름은 편집 가능하다. 각 단계는 순서, 상태, 담당자, 계획/실제 시간, 진행률, 선행 작업, 차단 사유, 의견을 가진다. 의뢰 상세 아래에 Open Cell 파손 분석과 Chassis Rear 영구변형 평가가 계층적으로 표시된다.

## 품질 및 안전

- DB 쿼리와 UI 컴포넌트를 분리한다.
- 위젯 정의와 자연어 변경 명세를 JSON Schema 또는 동등한 방식으로 검증한다.
- 자연어 입력으로 임의 SQL·명령·코드를 실행하지 않는다.
- 파일 형식·경로·크기·체크섬을 검증한다.
- 빈 데이터, 누락값, 잘못된 단위, 없는 파일을 안전하게 안내한다.
- 단위와 기준값을 차트에 명시하고 한국어 UI를 기본으로 한다.
- 샘플만으로 전체 기능을 실행할 수 있어야 한다.

## 실행 단계

1. 프로젝트 조사와 기술 구조 결정
2. 데이터 모델과 샘플 데이터
3. 백엔드 API와 DuckDB 저장소
4. 대시보드 조회 화면
5. 편집기와 위젯/변수 카탈로그
6. 레이아웃 저장·복제·복구
7. 자연어 변경 명세와 미리보기
8. 필수 예제 두 개
9. 테스트·시각 검증·문서화

각 단계 후 앱 실행, API/필터/저장/복구 테스트와 화면 검증을 수행한다. 중요한 충돌만 질문하며 나머지는 `assumptions.md`에 가정을 남기고 진행한다.

## 완료 조건

1. 새 환경에서 README 명령만으로 프런트엔드와 백엔드를 실행한다.
2. DuckDB와 샘플 데이터가 자동 준비된다.
3. TV 포장 낙하와 해석 의뢰 상태 예제가 실제 샘플 데이터를 표시한다.
4. 위젯 추가, 변수/표현 선택, 이동과 크기 변경이 된다.
5. 레이아웃 저장·불러오기·복제·최근 정상 버전 복구가 된다.
6. 자연어 변경 요청을 검증된 미리보기 후 적용할 수 있다.
7. 모든 필터·KPI·판정·차트·표가 데이터 쿼리에 연동된다.
8. 이미지·영상·경량 3D의 안전한 파일 메타데이터 구조가 있다.
9. 핵심 테스트와 실제 화면 검증이 통과한다.
10. README에 구조·실행법·데이터 모델·자연어 편집·파일 정책·PostgreSQL 전환법이 있다.
11. PPT 시각 편집기의 배치와 변수 연결이 실제 PPTX 좌표에 반영되고 레이아웃 버전으로 복원된다.
12. 규칙 기반 PPTX 템플릿 업로드·태그 검사·변수 치환과 위험 파일 거부가 동작한다.
