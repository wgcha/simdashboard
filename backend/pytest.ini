[pytest]
tmp_path_retention_policy = none
tmp_path_retention_count = 0
markers =
    unit: fast tests without application startup or an external database
    duckdb_integration: tests using the local DuckDB adapter
    postgres_integration: tests requiring an explicitly enabled PostgreSQL profile
    contract: API, schema, or adapter contract tests
    e2e: browser-level end-to-end tests
