*** Begin Patch
*** Update File: backend/app/main.py
@@
-from fastapi.middleware.cors import CORSMiddleware
+from fastapi.middleware.cors import CORSMiddleware
+from fastapi.staticfiles import StaticFiles
@@
 app.add_middleware(
@@
     allow_headers=["*"],
 )
+app.mount("/assets", StaticFiles(directory=str(__import__("pathlib").Path(__file__).resolve().parents[1] / "assets")), name="assets")

*** Add File: frontend/package.json
+{
+  "name": "analysis-canvas",
+  "private": true,
+  "version": "0.1.0",
+  "type": "module",
+  "scripts": {
+    "dev": "vite --host 0.0.0.0",
+    "build": "tsc -b && vite build",
+    "preview": "vite preview --host 0.0.0.0"
+  },
+  "dependencies": {
+    "lucide-react": "^0.468.0",
+    "react": "^18.3.1",
+    "react-dom": "^18.3.1",
+    "react-grid-layout": "^1.5.2",
+    "recharts": "^2.15.2"
+  },
+  "devDependencies": {
+    "@types/react": "^18.3.18",
+    "@types/react-dom": "^18.3.5",
+    "@types/react-grid-layout": "^1.3.5",
+    "@vitejs/plugin-react": "^4.4.1",
+    "typescript": "~5.7.2",
+    "vite": "^6.1.0"
+  }
+}

*** Add File: frontend/index.html
+<!doctype html>
+<html lang="ko">
+  <head>
+    <meta charset="UTF-8" />
+    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
+    <meta name="theme-color" content="#07111d" />
+    <title>Analysis Canvas</title>
+  </head>
+  <body>
+    <div id="root"></div>
+    <script type="module" src="/src/main.tsx"></script>
+  </body>
+</html>

*** Add File: frontend/tsconfig.json
+{
+  "files": [],
+  "references": [
+    { "path": "./tsconfig.app.json" },
+    { "path": "./tsconfig.node.json" }
+  ]
+}

*** Add File: frontend/tsconfig.app.json
+{
+  "compilerOptions": {
+    "target": "ES2022",
+    "useDefineForClassFields": true,
+    "lib": ["ES2022", "DOM", "DOM.Iterable"],
+    "allowJs": false,
+    "skipLibCheck": true,
+    "esModuleInterop": true,
+    "allowSyntheticDefaultImports": true,
+    "strict": true,
+    "forceConsistentCasingInFileNames": true,
+    "module": "ESNext",
+    "moduleResolution": "Bundler",
+    "resolveJsonModule": true,
+    "isolatedModules": true,
+    "noEmit": true,
+    "jsx": "react-jsx"
+  },
+  "include": ["src"]
+}

*** Add File: frontend/tsconfig.node.json
+{
+  "compilerOptions": {
+    "composite": true,
+    "skipLibCheck": true,
+    "module": "ESNext",
+    "moduleResolution": "Bundler",
+    "allowImportingTsExtensions": true
+  },
+  "include": ["vite.config.ts"]
+}

*** Add File: frontend/vite.config.ts
+import { defineConfig } from 'vite'
+import react from '@vitejs/plugin-react'
+
+export default defineConfig({
+  plugins: [react()],
+  server: {
+    port: 5173,
+    proxy: {
+      '/api': 'http://127.0.0.1:8000',
+      '/assets': 'http://127.0.0.1:8000',
+    },
+  },
+})

*** Add File: frontend/src/types.ts
+export type ScalarResult = {
+  id: string
+  variable_key: string
+  display_name: string
+  value_double: number
+  unit: string
+  threshold_double: number
+  verdict: 'PASS' | 'FAIL'
+}
+
+export type TimeSeriesPoint = {
+  variable_key: string
+  display_name: string
+  time_value: number
+  value: number
+  time_unit: string
+  value_unit: string
+}
+
+export type Overview = {
+  load_case: {
+    id: string
+    name: string
+    analysis_type: string
+    project_name: string
+    product_name: string
+    request_title: string
+    parameters: Record<string, string | number>
+  }
+  run: string
+  overall_verdict: 'PASS' | 'FAIL' | 'NO_DATA'
+  threshold: number
+  scalar_results: ScalarResult[]
+  time_series: TimeSeriesPoint[]
+  notes: Array<{ id: string; author: string; body: string; created_at: string }>
+  media: Array<{ id: string; title: string; file_path: string; mime_type: string }>
+  template_execution: {
+    template_name: string
+    template_version: string
+    generated_model: { elements: number; model: string }
+  }
+}
+
+export type WorkflowStep = {
+  id: string
+  sequence_no: number
+  name: string
+  status: 'COMPLETED' | 'IN_PROGRESS' | 'WAITING' | 'BLOCKED' | 'FAILED'
+  owner: string
+  progress: number
+  planned_end: string
+  note?: string
+}
+
+export type Workflow = {
+  request: { id: string; title: string; status: string; owner: string; due_at: string }
+  steps: WorkflowStep[]
+  progress: number
+}
+
+export type WidgetType = 'verdict' | 'edge_bar' | 'summary' | 'time_series' | 'note' | 'result_table' | 'contour'
+
+export type DashboardWidget = {
+  id: string
+  type: WidgetType
+  title: string
+  x: number
+  y: number
+  w: number
+  h: number
+  settings?: Record<string, unknown>
+}
+
+export type DashboardDefinition = {
+  id: string
+  name: string
+  description: string
+  widgets: DashboardWidget[]
+  version?: number
+  updated_at?: string
+}

*** Add File: frontend/src/api.ts
+import type { DashboardDefinition, Overview, Workflow } from './types'
+
+async function json<T>(response: Response): Promise<T> {
+  if (!response.ok) {
+    const detail = await response.text()
+    throw new Error(detail || `요청 실패 (${response.status})`)
+  }
+  return response.json() as Promise<T>
+}
+
+export const api = {
+  overview: () => json<Overview>(fetch('/api/load-cases/loadcase-drop-bottom-001/overview')),
+  workflow: () => json<Workflow>(fetch('/api/requests/request-drop-001/workflow')),
+  dashboard: () => json<DashboardDefinition>(fetch('/api/dashboards/dashboard-drop-default')),
+  saveDashboard: (definition: DashboardDefinition) =>
+    json<{ status: string; version: number; updated_at: string }>(
+      fetch(`/api/dashboards/${definition.id}`, {
+        method: 'PUT',
+        headers: { 'Content-Type': 'application/json' },
+        body: JSON.stringify(definition),
+      }),
+    ),
+  previewCommand: (command: string) =>
+    json<{
+      recognized: boolean
+      message: string
+      proposal: null | { action: 'add_widget'; widget: DashboardDefinition['widgets'][number] }
+    }>(
+      fetch('/api/dashboard-commands/preview', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        body: JSON.stringify({ command }),
+      }),
+    ),
+}

*** Add File: frontend/src/main.tsx
+import React from 'react'
+import ReactDOM from 'react-dom/client'
+import App from './App'
+import './styles.css'
+import 'react-grid-layout/css/styles.css'
+import 'react-resizable/css/styles.css'
+
+ReactDOM.createRoot(document.getElementById('root')!).render(
+  <React.StrictMode>
+    <App />
+  </React.StrictMode>,
+)

*** Add File: frontend/src/App.tsx
+import { useEffect, useMemo, useState } from 'react'
+import {
+  Activity,
+  AlertTriangle,
+  BarChart3,
+  Check,
+  ChevronDown,
+  CircleDot,
+  Database,
+  GripVertical,
+  LayoutDashboard,
+  LoaderCircle,
+  Lock,
+  MessageSquareText,
+  PanelLeftClose,
+  Play,
+  Plus,
+  Save,
+  Settings2,
+  Sparkles,
+  X,
+} from 'lucide-react'
+import { WidthProvider, Responsive, type Layout, type Layouts } from 'react-grid-layout'
+import {
+  Area,
+  AreaChart,
+  Bar,
+  BarChart,
+  CartesianGrid,
+  Cell,
+  Legend,
+  Line,
+  LineChart,
+  ReferenceLine,
+  ResponsiveContainer,
+  Tooltip,
+  XAxis,
+  YAxis,
+} from 'recharts'
+import { api } from './api'
+import type { DashboardDefinition, DashboardWidget, Overview, Workflow, WorkflowStep } from './types'
+
+const ResponsiveGridLayout = WidthProvider(Responsive)
+const LOAD_CASE_ID = 'loadcase-drop-bottom-001'
+const SERIES_COLORS = ['#61d4ff', '#ff647d', '#70e0a8', '#ffbf57']
+
+function App() {
+  const [overview, setOverview] = useState<Overview | null>(null)
+  const [workflow, setWorkflow] = useState<Workflow | null>(null)
+  const [dashboard, setDashboard] = useState<DashboardDefinition | null>(null)
+  const [activeView, setActiveView] = useState<'analysis' | 'workflow'>('analysis')
+  const [editMode, setEditMode] = useState(false)
+  const [assistantOpen, setAssistantOpen] = useState(false)
+  const [command, setCommand] = useState('')
+  const [proposal, setProposal] = useState<Awaited<ReturnType<typeof api.previewCommand>> | null>(null)
+  const [notice, setNotice] = useState('')
+  const [error, setError] = useState('')
+  const [loading, setLoading] = useState(true)
+  const [selectedEdges, setSelectedEdges] = useState<string[]>(['top', 'bottom', 'left', 'right'])
+
+  useEffect(() => {
+    Promise.all([api.overview(), api.workflow(), api.dashboard()])
+      .then(([overviewData, workflowData, dashboardData]) => {
+        setOverview(overviewData)
+        setWorkflow(workflowData)
+        setDashboard(dashboardData)
+      })
+      .catch((reason: Error) => setError(reason.message))
+      .finally(() => setLoading(false))
+  }, [])
+
+  const handleLayoutChange = (_current: Layout[], layouts: Layouts) => {
+    if (!dashboard || !editMode) return
+    const positions = new Map((layouts.lg ?? []).map((item) => [item.i, item]))
+    setDashboard({
+      ...dashboard,
+      widgets: dashboard.widgets.map((widget) => {
+        const position = positions.get(widget.id)
+        return position
+          ? { ...widget, x: position.x, y: position.y, w: position.w, h: position.h }
+          : widget
+      }),
+    })
+  }
+
+  const save = async () => {
+    if (!dashboard) return
+    try {
+      const result = await api.saveDashboard(dashboard)
+      setDashboard({ ...dashboard, version: result.version, updated_at: result.updated_at })
+      setNotice(`레이아웃 v${result.version} 저장 완료`)
+      setTimeout(() => setNotice(''), 2600)
+      setEditMode(false)
+    } catch (reason) {
+      setError(reason instanceof Error ? reason.message : '저장하지 못했습니다.')
+    }
+  }
+
+  const previewCommand = async () => {
+    if (!command.trim()) return
+    try {
+      setProposal(await api.previewCommand(command))
+    } catch (reason) {
+      setError(reason instanceof Error ? reason.message : '요청을 해석하지 못했습니다.')
+    }
+  }
+
+  const applyProposal = () => {
+    const widget = proposal?.proposal?.widget
+    if (!dashboard || !widget) return
+    setDashboard({ ...dashboard, widgets: [...dashboard.widgets, widget] })
+    setProposal(null)
+    setCommand('')
+    setAssistantOpen(false)
+    setEditMode(true)
+    setNotice('변경안을 적용했습니다. 저장하면 새 버전이 생성됩니다.')
+    setTimeout(() => setNotice(''), 3000)
+  }
+
+  const removeWidget = (id: string) => {
+    if (!dashboard) return
+    setDashboard({ ...dashboard, widgets: dashboard.widgets.filter((item) => item.id !== id) })
+  }
+
+  if (loading) {
+    return <div className="full-state"><LoaderCircle className="spin" /> 데이터와 레이아웃을 준비하고 있습니다.</div>
+  }
+
+  if (error || !overview || !workflow || !dashboard) {
+    return <div className="full-state error"><AlertTriangle /> {error || '대시보드를 불러오지 못했습니다.'}</div>
+  }
+
+  return (
+    <div className="app-shell">
+      <aside className="sidebar">
+        <div className="brand"><span className="brand-mark"><Activity /></span><span>ANALYSIS<br /><strong>CANVAS</strong></span></div>
+        <nav className="nav-main">
+          <button className="active"><LayoutDashboard /><span>대시보드</span></button>
+          <button><Database /><span>해석 데이터</span></button>
+          <button><BarChart3 /><span>변수 카탈로그</span></button>
+          <button><Settings2 /><span>자동화 템플릿</span></button>
+        </nav>
+        <div className="sidebar-foot">
+          <div className="system-pill"><span className="live-dot" /> DUCKDB · LOCAL</div>
+          <button><PanelLeftClose /> 메뉴 접기</button>
+        </div>
+      </aside>
+
+      <main className="main-shell">
+        <header className="topbar">
+          <div className="breadcrumb"><span>프로젝트</span><b>/</b><span>{overview.load_case.project_name}</span><b>/</b><strong>{overview.load_case.name}</strong></div>
+          <div className="top-actions">
+            <button className="ghost-button" onClick={() => setAssistantOpen(true)}><Sparkles /> 자연어로 개선</button>
+            {editMode ? (
+              <button className="primary-button" onClick={save}><Save /> 레이아웃 저장</button>
+            ) : (
+              <button className="edit-button" onClick={() => setEditMode(true)}><Settings2 /> 대시보드 편집</button>
+            )}
+            <div className="avatar">HK</div>
+          </div>
+        </header>
+
+        <section className="content-head">
+          <div>
+            <div className="eyebrow"><span>PROJECT 24-071</span><span>•</span><span>{overview.load_case.analysis_type}</span></div>
+            <h1>{dashboard.name}</h1>
+            <p>{overview.load_case.request_title}</p>
+          </div>
+          <div className="head-meta">
+            <div><span>하중 경우</span><button>{overview.load_case.name}<ChevronDown /></button></div>
+            <div className="version"><span>레이아웃</span><strong>VERSION {dashboard.version ?? 1}</strong></div>
+          </div>
+        </section>
+
+        <section className="view-tabs">
+          <button className={activeView === 'analysis' ? 'active' : ''} onClick={() => setActiveView('analysis')}><Activity /> 낙하 해석</button>
+          <button className={activeView === 'workflow' ? 'active' : ''} onClick={() => setActiveView('workflow')}><CircleDot /> 의뢰 진행 상태 <span>{workflow.progress}%</span></button>
+          <div className="tab-line" />
+          {activeView === 'analysis' && <EdgeFilter selected={selectedEdges} setSelected={setSelectedEdges} />}
+        </section>
+
+        {editMode && (
+          <div className="edit-banner"><GripVertical /><span><strong>편집 모드</strong> 위젯을 드래그하거나 모서리를 잡아 크기를 조절하세요.</span><button onClick={() => setEditMode(false)}>편집 취소</button></div>
+        )}
+
+        <section className="canvas-area">
+          {activeView === 'analysis' ? (
+            <ResponsiveGridLayout
+              className="layout"
+              layouts={{ lg: dashboard.widgets.map((item) => ({ i: item.id, x: item.x, y: item.y, w: item.w, h: item.h })) }}
+              breakpoints={{ lg: 1180, md: 760, sm: 0 }}
+              cols={{ lg: 12, md: 8, sm: 1 }}
+              rowHeight={74}
+              margin={[16, 16]}
+              isDraggable={editMode}
+              isResizable={editMode}
+              compactType="vertical"
+              onLayoutChange={handleLayoutChange}
+            >
+              {dashboard.widgets.map((widget) => (
+                <div key={widget.id}>
+                  <WidgetCard widget={widget} overview={overview} selectedEdges={selectedEdges} editMode={editMode} onRemove={() => removeWidget(widget.id)} />
+                </div>
+              ))}
+            </ResponsiveGridLayout>
+          ) : (
+            <WorkflowView workflow={workflow} />
+          )}
+        </section>
+      </main>
+
+      {assistantOpen && (
+        <div className="drawer-backdrop" onMouseDown={() => setAssistantOpen(false)}>
+          <aside className="assistant-drawer" onMouseDown={(event) => event.stopPropagation()}>
+            <div className="drawer-head"><div><span><Sparkles /></span><div><strong>Canvas Copilot</strong><small>자연어 대시보드 편집</small></div></div><button onClick={() => setAssistantOpen(false)}><X /></button></div>
+            <div className="assistant-copy"><h2>어떤 시각화가 필요하세요?</h2><p>등록된 변수와 허용된 위젯만 사용해 안전한 변경안을 만듭니다. 적용 전 내용을 확인할 수 있습니다.</p></div>
+            <div className="suggestions">
+              {['응력-시간 그래프를 추가해', '상하좌우 최대 응력 막대그래프를 추가해', '패스/실패 판정 카드를 추가해'].map((text) => <button key={text} onClick={() => setCommand(text)}>{text}<Plus /></button>)}
+            </div>
+            <textarea value={command} onChange={(event) => setCommand(event.target.value)} placeholder="예: 응력-시간 그래프에 기준선을 넣어줘" />
+            <button className="assistant-submit" onClick={previewCommand} disabled={!command.trim()}><Sparkles /> 변경안 만들기</button>
+            {proposal && (
+              <div className={`proposal ${proposal.recognized ? 'recognized' : ''}`}>
+                <span>{proposal.recognized ? <Check /> : <AlertTriangle />}</span>
+                <div><strong>{proposal.recognized ? '적용 전 미리보기' : '요청 확인 필요'}</strong><p>{proposal.message}</p>{proposal.proposal && <code>{proposal.proposal.widget.title} · {proposal.proposal.widget.type}</code>}</div>
+                {proposal.recognized && <button onClick={applyProposal}>변경안 적용</button>}
+              </div>
+            )}
+            <div className="assistant-safe"><Lock /><span><strong>안전한 변경</strong>자연어 명령은 SQL이나 코드를 직접 실행하지 않습니다.</span></div>
+          </aside>
+        </div>
+      )}
+      {notice && <div className="toast"><Check /> {notice}</div>}
+    </div>
+  )
+}
+
+function EdgeFilter({ selected, setSelected }: { selected: string[]; setSelected: (value: string[]) => void }) {
+  const options = [['top', '상단'], ['bottom', '하단'], ['left', '좌측'], ['right', '우측']]
+  const toggle = (key: string) => setSelected(selected.includes(key) ? selected.filter((item) => item !== key) : [...selected, key])
+  return <div className="edge-filter"><span>표시 엣지</span>{options.map(([key, label]) => <button className={selected.includes(key) ? 'on' : ''} key={key} onClick={() => toggle(key)}><i />{label}</button>)}</div>
+}
+
+function WidgetCard({ widget, overview, selectedEdges, editMode, onRemove }: { widget: DashboardWidget; overview: Overview; selectedEdges: string[]; editMode: boolean; onRemove: () => void }) {
+  return (
+    <article className={`widget-card widget-${widget.type} ${editMode ? 'editable' : ''}`}>
+      <header><div><span className="widget-kicker">{widget.type.replace('_', ' ')}</span><h3>{widget.title}</h3></div>{editMode ? <button className="remove-widget" onClick={onRemove}><X /></button> : <button className="widget-menu">•••</button>}</header>
+      <div className="widget-body"><WidgetContent type={widget.type} overview={overview} selectedEdges={selectedEdges} /></div>
+    </article>
+  )
+}
+
+function WidgetContent({ type, overview, selectedEdges }: { type: DashboardWidget['type']; overview: Overview; selectedEdges: string[] }) {
+  const filteredScalars = overview.scalar_results.filter((item) => selectedEdges.some((edge) => item.variable_key.startsWith(edge)))
+  const edgeLabel = (key: string) => ({ top: '상단', bottom: '하단', left: '좌측', right: '우측' }[key.split('_')[0]] ?? key)
+  const barData = filteredScalars.map((item) => ({ name: edgeLabel(item.variable_key), value: item.value_double, verdict: item.verdict }))
+  const seriesData = useMemo(() => {
+    const grouped = new Map<number, Record<string, number>>()
+    overview.time_series.filter((item) => selectedEdges.some((edge) => item.variable_key.startsWith(edge))).forEach((item) => {
+      const point = grouped.get(item.time_value) ?? { time: item.time_value }
+      point[item.variable_key] = item.value
+      grouped.set(item.time_value, point)
+    })
+    return [...grouped.values()]
+  }, [overview.time_series, selectedEdges])
+
+  if (type === 'verdict') return <div className={`verdict-block ${overview.overall_verdict.toLowerCase()}`}><div className="verdict-icon">{overview.overall_verdict === 'PASS' ? <Check /> : <X />}</div><div><strong>{overview.overall_verdict}</strong><span>{overview.overall_verdict === 'PASS' ? '허용 기준 만족' : '기준 초과 감지'}</span></div><small>LIMIT {overview.threshold} MPa</small></div>
+  if (type === 'summary') return <div className="summary-grid"><div><span>낙하 높이</span><strong>{overview.load_case.parameters.drop_height_mm}<em>mm</em></strong></div><div><span>낙하 방향</span><strong>{overview.load_case.parameters.direction}</strong></div><div><span>자동화 템플릿</span><strong>{overview.template_execution.template_version}</strong></div><div><span>요소 수</span><strong>{overview.template_execution.generated_model.elements.toLocaleString()}</strong></div></div>
+  if (type === 'edge_bar') return <ResponsiveContainer width="100%" height="100%"><BarChart data={barData} margin={{ top: 12, right: 18, left: -12, bottom: 0 }}><CartesianGrid vertical={false} stroke="#26394c" strokeDasharray="3 3"/><XAxis dataKey="name" tick={{ fill: '#8fa6bb', fontSize: 12 }} axisLine={false} tickLine={false}/><YAxis domain={[0, 100]} tick={{ fill: '#6f879d', fontSize: 11 }} axisLine={false} tickLine={false} unit=""/><Tooltip contentStyle={{ background: '#102235', border: '1px solid #2d465c', borderRadius: 10 }} formatter={(value: number) => [`${value} MPa`, '최대 응력']}/><ReferenceLine y={overview.threshold} stroke="#ffbf57" strokeDasharray="5 5" label={{ value: `기준 ${overview.threshold}`, fill: '#ffbf57', fontSize: 11, position: 'insideTopRight' }}/><Bar dataKey="value" radius={[5,5,1,1]}>{barData.map((entry) => <Cell key={entry.name} fill={entry.verdict === 'FAIL' ? '#ff5d73' : '#4fd6a0'} />)}</Bar></BarChart></ResponsiveContainer>
+  if (type === 'time_series') return <ResponsiveContainer width="100%" height="100%"><LineChart data={seriesData} margin={{ top: 10, right: 22, left: -8, bottom: 2 }}><CartesianGrid stroke="#24384b" strokeDasharray="3 3"/><XAxis dataKey="time" tick={{ fill: '#71899f', fontSize: 11 }} axisLine={{ stroke: '#31485b' }} tickLine={false} label={{ value: 'TIME (ms)', fill: '#6f879d', fontSize: 10, position: 'insideBottomRight', offset: -2 }}/><YAxis domain={[0, 95]} tick={{ fill: '#71899f', fontSize: 11 }} axisLine={false} tickLine={false}/><Tooltip contentStyle={{ background: '#102235', border: '1px solid #2d465c', borderRadius: 10 }}/><Legend wrapperStyle={{ fontSize: 11, paddingTop: 5 }}/><ReferenceLine y={overview.threshold} stroke="#ffbf57" strokeDasharray="6 4"/>{selectedEdges.map((edge, index) => <Line key={edge} type="monotone" dataKey={`${edge}_edge_stress_time`} name={`${edgeLabel(edge)} 엣지`} dot={false} stroke={SERIES_COLORS[index]} strokeWidth={2}/>)}</LineChart></ResponsiveContainer>
+  if (type === 'note') return <div className="note-block"><MessageSquareText /><blockquote>{overview.notes[0]?.body ?? '등록된 의견이 없습니다.'}</blockquote><footer><span>{overview.notes[0]?.author ?? '-'}</span><small>ANALYSIS ENGINEER</small></footer></div>
+  if (type === 'result_table') return <div className="result-table"><div className="table-head"><span>측정 위치</span><span>최대 응력</span><span>허용 기준</span><span>여유율</span><span>판정</span></div>{filteredScalars.map((item) => <div className="table-row" key={item.id}><strong><i className={`edge-${item.variable_key.split('_')[0]}`} />{item.display_name.replace(' 최대 응력','')}</strong><span>{item.value_double.toFixed(1)} <small>{item.unit}</small></span><span>{item.threshold_double.toFixed(1)} <small>{item.unit}</small></span><span className={item.verdict === 'FAIL' ? 'negative' : 'positive'}>{((item.threshold_double - item.value_double) / item.threshold_double * 100).toFixed(1)}%</span><b className={item.verdict.toLowerCase()}>{item.verdict}</b></div>)}</div>
+  if (type === 'contour') return <div className="contour"><img src="/assets/sample-contour.svg" alt="Open Cell 응력 컨투어" /></div>
+  return <div className="empty-widget">표시할 데이터가 없습니다.</div>
+}
+
+function WorkflowView({ workflow }: { workflow: Workflow }) {
+  return <div className="workflow-layout"><section className="workflow-hero"><div><span>ANALYSIS REQUEST</span><h2>{workflow.request.title}</h2><p>담당자 {workflow.request.owner} · 목표 완료 {new Date(workflow.request.due_at).toLocaleDateString('ko-KR')}</p></div><div className="progress-ring"><strong>{workflow.progress}%</strong><span>전체 진행률</span></div></section><section className="workflow-card"><header><div><span className="widget-kicker">WORKFLOW STATUS</span><h3>의뢰 작업 순서</h3></div><span className="workflow-live"><i /> 결과 검토 진행 중</span></header><div className="workflow-steps">{workflow.steps.map((step, index) => <WorkflowStepItem key={step.id} step={step} last={index === workflow.steps.length - 1} />)}</div></section></div>
+}
+
+function WorkflowStepItem({ step, last }: { step: WorkflowStep; last: boolean }) {
+  const statusText = { COMPLETED: '완료', IN_PROGRESS: '진행 중', WAITING: '대기', BLOCKED: '차단', FAILED: '실패' }[step.status]
+  return <div className={`workflow-step ${step.status.toLowerCase()}`}><div className="step-track"><span>{step.status === 'COMPLETED' ? <Check /> : step.sequence_no}</span>{!last && <i />}</div><div className="step-copy"><strong>{step.name}</strong><span>{step.owner}</span>{step.note && <small>{step.note}</small>}</div><div className="step-progress"><i><b style={{ width: `${step.progress}%` }} /></i><span>{step.progress}%</span></div><b className="status-badge">{statusText}</b></div>
+}
+
+export default App

*** Add File: frontend/src/styles.css
+@import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600&family=Noto+Sans+KR:wght@400;500;600;700;800&display=swap');
+
+:root { font-family: 'Noto Sans KR', sans-serif; color: #e9f3fc; background: #07111d; font-synthesis: none; --panel:#0d1c2b; --line:#213549; --muted:#8198ad; --cyan:#51d3ff; --green:#4fd6a0; --red:#ff5d73; --amber:#ffbf57; }
+* { box-sizing: border-box; }
+body { margin: 0; min-width: 320px; min-height: 100vh; background: radial-gradient(circle at 42% -20%, #163555 0, #07111d 42%); }
+button, textarea { font: inherit; }
+button { color: inherit; }
+.app-shell { min-height:100vh; display:flex; }
+.sidebar { position:fixed; inset:0 auto 0 0; width:224px; padding:24px 17px 18px; background:rgba(6,15,25,.96); border-right:1px solid #1b2e40; display:flex; flex-direction:column; z-index:20; }
+.brand { display:flex; align-items:center; gap:11px; padding:0 8px 28px; color:#8ba2b7; font:500 10px/1.15 'IBM Plex Mono'; letter-spacing:2px; }
+.brand strong { color:#f1f8ff; font-size:14px; letter-spacing:3px; }
+.brand-mark { width:36px; height:36px; display:grid; place-items:center; color:#07111d; background:linear-gradient(145deg,#7ce4ff,#45b9ee); clip-path:polygon(15% 0,100% 0,85% 100%,0 100%); }
+.brand-mark svg { width:19px; }
+.nav-main { display:grid; gap:7px; }
+.nav-main button,.sidebar-foot button { border:0; background:transparent; color:#8096aa; border-radius:8px; height:44px; display:flex; align-items:center; gap:12px; padding:0 13px; cursor:pointer; text-align:left; font-size:13px; }
+.nav-main button svg,.sidebar-foot button svg { width:17px; }
+.nav-main button:hover,.nav-main button.active { background:#10263a; color:#edf8ff; }
+.nav-main button.active { box-shadow:inset 3px 0 #4ecdf8; }
+.sidebar-foot { margin-top:auto; display:grid; gap:8px; }
+.system-pill { padding:10px 12px; color:#6f879b; border:1px solid #1d3347; border-radius:7px; font:600 9px 'IBM Plex Mono'; letter-spacing:1.2px; }
+.live-dot { display:inline-block; width:6px; height:6px; background:var(--green); border-radius:50%; margin-right:7px; box-shadow:0 0 8px var(--green); }
+.main-shell { margin-left:224px; width:calc(100% - 224px); min-width:0; }
+.topbar { height:68px; padding:0 28px; border-bottom:1px solid #1c3042; background:rgba(7,17,29,.82); backdrop-filter:blur(16px); display:flex; align-items:center; justify-content:space-between; position:sticky; top:0; z-index:15; }
+.breadcrumb { display:flex; gap:9px; align-items:center; font-size:11px; color:#687f94; white-space:nowrap; overflow:hidden; }
+.breadcrumb b { color:#345069; }.breadcrumb strong { color:#a8bdcf; overflow:hidden; text-overflow:ellipsis; }
+.top-actions { display:flex; align-items:center; gap:9px; }
+.top-actions button { height:36px; border-radius:7px; padding:0 13px; display:flex; align-items:center; gap:7px; cursor:pointer; font-size:11px; font-weight:700; }
+.top-actions svg { width:15px; }.ghost-button { background:transparent; border:1px solid #254057; color:#a7bdd0; }.ghost-button:hover { border-color:#4dcffb; color:#e9f9ff; }
+.edit-button { background:#132d44; border:1px solid #31536d; }.primary-button { color:#03141e; background:var(--cyan); border:1px solid var(--cyan); }
+.avatar { width:34px; height:34px; border-radius:50%; background:#203b51; display:grid; place-items:center; color:#b9d8e9; font:600 10px 'IBM Plex Mono'; margin-left:5px; }
+.content-head { padding:30px 32px 24px; display:flex; align-items:flex-end; justify-content:space-between; gap:30px; }
+.eyebrow { display:flex; align-items:center; gap:9px; color:#59cbed; font:600 9px 'IBM Plex Mono'; letter-spacing:1.2px; margin-bottom:9px; }
+.content-head h1 { margin:0; font-size:27px; letter-spacing:-.8px; }.content-head p { margin:7px 0 0; color:#6f879d; font-size:12px; }
+.head-meta { display:flex; align-items:end; gap:22px; }.head-meta>div { display:grid; gap:6px; }.head-meta span { color:#60788e; font:600 8px 'IBM Plex Mono'; letter-spacing:1px; }
+.head-meta button { height:34px; min-width:220px; display:flex; align-items:center; justify-content:space-between; gap:10px; background:#0d2031; border:1px solid #263f55; border-radius:6px; color:#b8ccdc; padding:0 10px; font-size:10px; }.head-meta svg { width:13px; }
+.version strong { height:34px; display:flex; align-items:center; color:#60d1f5; font:600 10px 'IBM Plex Mono'; }
+.view-tabs { height:48px; padding:0 32px; display:flex; align-items:end; gap:2px; border-bottom:1px solid #1c3042; }
+.view-tabs>button { height:48px; padding:0 17px; border:0; border-bottom:2px solid transparent; background:transparent; color:#70879c; display:flex; align-items:center; gap:8px; cursor:pointer; font-size:11px; }
+.view-tabs>button svg { width:15px; }.view-tabs>button.active { color:#dff6ff; border-bottom-color:var(--cyan); }.view-tabs>button span { background:#19354b; color:#67d6fa; padding:2px 6px; border-radius:8px; font:600 8px 'IBM Plex Mono'; }
+.tab-line { flex:1; }.edge-filter { height:48px; display:flex; align-items:center; gap:6px; }.edge-filter>span { color:#5e778c; font-size:9px; margin-right:6px; }.edge-filter button { height:25px; padding:0 9px; border:1px solid #263e53; border-radius:12px; background:transparent; color:#637c91; font-size:9px; cursor:pointer; }.edge-filter button i { display:inline-block; width:5px; height:5px; border-radius:50%; background:#516a7d; margin-right:5px; }.edge-filter button.on { color:#bcd5e7; background:#11283a; }.edge-filter button.on i { background:var(--cyan); box-shadow:0 0 5px var(--cyan); }
+.edit-banner { margin:16px 32px 0; min-height:44px; border:1px dashed #3282a3; background:#0c2538; border-radius:8px; padding:0 14px; display:flex; align-items:center; gap:11px; color:#91c8dc; font-size:11px; }.edit-banner svg { width:16px; }.edit-banner strong { color:#e5f9ff; margin-right:7px; }.edit-banner button { margin-left:auto; border:0; background:transparent; color:#87aabe; cursor:pointer; font-size:10px; }
+.canvas-area { padding:14px 16px 40px; }.layout { min-height:700px; }.react-grid-item.react-grid-placeholder { background:#31c6fa; opacity:.18; border-radius:9px; }.react-resizable-handle::after { border-color:#58d5ff !important; }
+.widget-card { height:100%; background:linear-gradient(145deg,rgba(14,30,45,.98),rgba(10,24,37,.98)); border:1px solid #20364a; border-radius:9px; box-shadow:0 10px 26px rgba(0,0,0,.12); overflow:hidden; display:flex; flex-direction:column; transition:border-color .2s; }.widget-card.editable { border-color:#33779a; cursor:move; }.widget-card>header { height:58px; min-height:58px; padding:0 16px; border-bottom:1px solid #1a3042; display:flex; align-items:center; justify-content:space-between; }.widget-kicker { display:block; color:#4f6f87; font:600 7px 'IBM Plex Mono'; letter-spacing:1.2px; text-transform:uppercase; margin-bottom:4px; }.widget-card h3,.workflow-card h3 { margin:0; font-size:12px; font-weight:600; color:#cfe0ec; }.widget-menu,.remove-widget { border:0; background:transparent; color:#587186; cursor:pointer; letter-spacing:2px; }.remove-widget svg { width:15px; }.widget-body { flex:1; min-height:0; padding:10px 13px 13px; }
+.verdict-block { height:100%; display:grid; grid-template-columns:52px 1fr; grid-template-rows:1fr auto; align-items:center; gap:0 12px; padding:2px 8px; }.verdict-icon { grid-row:1 / 3; width:48px; height:48px; border-radius:50%; display:grid; place-items:center; }.verdict-icon svg { width:24px; stroke-width:3; }.verdict-block strong { display:block; font:700 27px 'IBM Plex Mono'; letter-spacing:2px; }.verdict-block span { display:block; color:#8199ad; font-size:9px; }.verdict-block small { color:#5e778d; font:600 8px 'IBM Plex Mono'; }.verdict-block.fail .verdict-icon { color:var(--red); background:rgba(255,93,115,.12); box-shadow:inset 0 0 0 1px rgba(255,93,115,.4); }.verdict-block.fail strong { color:var(--red); }.verdict-block.pass .verdict-icon { color:var(--green); background:rgba(79,214,160,.12); }.verdict-block.pass strong { color:var(--green); }
+.summary-grid { height:100%; display:grid; grid-template-columns:1fr 1fr; gap:11px 18px; padding:4px 5px; }.summary-grid div { display:flex; flex-direction:column; gap:3px; }.summary-grid span { color:#60798f; font-size:8px; }.summary-grid strong { color:#d9e8f2; font:600 14px 'IBM Plex Mono'; }.summary-grid em { color:#6f8ca1; font-size:8px; font-style:normal; margin-left:4px; }
+.note-block { height:100%; display:grid; grid-template-rows:auto 1fr auto; padding:7px 8px; }.note-block>svg { color:#3d91b4; width:20px; }.note-block blockquote { margin:10px 0; color:#9db2c3; font-size:10px; line-height:1.8; overflow:auto; }.note-block footer { border-top:1px solid #1d3447; padding-top:8px; display:grid; }.note-block footer span { color:#d7e8f3; font-size:9px; }.note-block footer small { color:#587187; font:600 7px 'IBM Plex Mono'; margin-top:2px; }
+.result-table { min-width:620px; }.table-head,.table-row { display:grid; grid-template-columns:1.6fr 1fr 1fr .8fr .7fr; align-items:center; min-height:40px; padding:0 10px; }.table-head { min-height:31px; color:#526d83; font:600 7px 'IBM Plex Mono'; letter-spacing:.8px; border-bottom:1px solid #21384b; }.table-row { color:#9bb0c1; font-size:9px; border-bottom:1px solid #172d3f; }.table-row:last-child { border:0; }.table-row strong { display:flex; align-items:center; gap:8px; color:#c1d3df; font-weight:500; }.table-row strong i { width:6px; height:6px; background:var(--cyan); border-radius:50%; }.table-row small { color:#5f778b; font-size:7px; }.table-row .positive { color:var(--green); }.table-row .negative { color:var(--red); }.table-row>b { justify-self:start; padding:3px 8px; border-radius:9px; font:600 8px 'IBM Plex Mono'; }.table-row>b.pass { color:var(--green); background:rgba(79,214,160,.1); }.table-row>b.fail { color:var(--red); background:rgba(255,93,115,.1); }.contour { height:100%; }.contour img { width:100%; height:100%; object-fit:cover; border-radius:5px; }.empty-widget { display:grid; place-items:center; height:100%; color:#617b91; font-size:10px; }
+.workflow-layout { max-width:1280px; margin:0 auto; padding:2px 16px; }.workflow-hero { min-height:125px; margin:0 0 16px; padding:24px 27px; border:1px solid #21394d; border-radius:10px; background:linear-gradient(110deg,#0e2234,#0c1b2a); display:flex; align-items:center; justify-content:space-between; }.workflow-hero>div>span { color:#4bbfe8; font:600 8px 'IBM Plex Mono'; letter-spacing:1.5px; }.workflow-hero h2 { margin:8px 0 5px; font-size:18px; }.workflow-hero p { margin:0; color:#718ba0; font-size:10px; }.progress-ring { width:82px; height:82px; border-radius:50%; border:7px solid #19344a; border-top-color:var(--cyan); display:grid; place-content:center; text-align:center; }.progress-ring strong { font:700 17px 'IBM Plex Mono'; }.progress-ring span { color:#6e879b; font-size:7px; }
+.workflow-card { background:#0d1d2c; border:1px solid #20374a; border-radius:10px; overflow:hidden; }.workflow-card>header { height:65px; padding:0 20px; display:flex; align-items:center; justify-content:space-between; border-bottom:1px solid #20364a; }.workflow-live { color:#8facbf; font-size:9px; }.workflow-live i { display:inline-block; width:6px; height:6px; background:var(--amber); border-radius:50%; margin-right:6px; box-shadow:0 0 8px var(--amber); }.workflow-steps { padding:11px 22px 18px; }.workflow-step { min-height:67px; display:grid; grid-template-columns:38px 1fr 220px 76px; align-items:center; gap:12px; }.step-track { height:67px; position:relative; display:flex; justify-content:center; }.step-track>span { margin-top:14px; width:24px; height:24px; border-radius:50%; background:#152b3e; border:1px solid #314c62; display:grid; place-items:center; color:#668097; font:600 9px 'IBM Plex Mono'; z-index:1; }.step-track svg { width:13px; }.step-track>i { position:absolute; top:38px; bottom:-15px; width:1px; background:#284156; }.completed .step-track>span { color:#061d17; background:var(--green); border-color:var(--green); }.in_progress .step-track>span { color:#06212c; background:var(--cyan); border-color:var(--cyan); box-shadow:0 0 13px rgba(81,211,255,.3); }.completed .step-track>i { background:#2d8a70; }.step-copy { display:grid; gap:3px; }.step-copy strong { color:#cadbe7; font-size:10px; }.step-copy>span { color:#607b90; font-size:8px; }.step-copy small { color:var(--amber); font-size:8px; }.step-progress { display:flex; align-items:center; gap:9px; }.step-progress>i { width:150px; height:4px; border-radius:3px; overflow:hidden; background:#1b3346; }.step-progress b { display:block; height:100%; background:#3dbb94; }.in_progress .step-progress b { background:var(--cyan); }.step-progress span { color:#658095; font:600 8px 'IBM Plex Mono'; }.status-badge { justify-self:end; min-width:60px; text-align:center; padding:5px 7px; border-radius:11px; color:#617b91; background:#13283a; font-size:8px; }.completed .status-badge { color:var(--green); background:rgba(79,214,160,.08); }.in_progress .status-badge { color:var(--cyan); background:rgba(81,211,255,.09); }
+.drawer-backdrop { position:fixed; inset:0; background:rgba(0,7,14,.65); backdrop-filter:blur(3px); z-index:50; }.assistant-drawer { position:absolute; inset:0 0 0 auto; width:min(430px,100%); background:#0a1825; border-left:1px solid #294054; box-shadow:-24px 0 60px rgba(0,0,0,.35); padding:20px; overflow:auto; }.drawer-head { display:flex; align-items:center; justify-content:space-between; padding-bottom:18px; border-bottom:1px solid #1d3346; }.drawer-head>div { display:flex; align-items:center; gap:11px; }.drawer-head>div>span { width:36px; height:36px; display:grid; place-items:center; border-radius:8px; background:#11364e; color:#5bd6ff; }.drawer-head svg { width:18px; }.drawer-head strong,.drawer-head small { display:block; }.drawer-head strong { font-size:12px; }.drawer-head small { color:#657f94; font-size:8px; margin-top:3px; }.drawer-head>button { border:0; background:transparent; color:#627b90; cursor:pointer; }.assistant-copy { margin:28px 2px 20px; }.assistant-copy h2 { margin:0; font-size:20px; }.assistant-copy p { color:#748da2; font-size:10px; line-height:1.7; }.suggestions { display:grid; gap:7px; margin-bottom:13px; }.suggestions button { min-height:38px; border:1px solid #203c51; background:#0d2131; border-radius:7px; color:#91abba; font-size:9px; padding:0 11px; display:flex; align-items:center; justify-content:space-between; cursor:pointer; }.suggestions button:hover { border-color:#3c9abd; }.suggestions svg { width:13px; }.assistant-drawer textarea { width:100%; min-height:112px; padding:13px; resize:vertical; border:1px solid #2a485e; outline:0; border-radius:8px; background:#07131e; color:#d8e9f4; font-size:11px; line-height:1.7; }.assistant-drawer textarea:focus { border-color:#4bcdfb; box-shadow:0 0 0 3px rgba(75,205,251,.07); }.assistant-submit { width:100%; height:42px; margin-top:10px; border:0; border-radius:7px; color:#03131c; background:var(--cyan); display:flex; align-items:center; justify-content:center; gap:7px; font-size:10px; font-weight:800; cursor:pointer; }.assistant-submit:disabled { opacity:.35; cursor:not-allowed; }.assistant-submit svg { width:15px; }.proposal { margin-top:16px; padding:13px; border:1px solid #5a3e36; background:#251b1d; border-radius:8px; display:grid; grid-template-columns:24px 1fr; gap:8px; color:#f1a181; }.proposal.recognized { color:#6eddb0; background:#0d2823; border-color:#23594a; }.proposal svg { width:17px; }.proposal strong { font-size:10px; }.proposal p { margin:4px 0 8px; color:#9eb3c0; font-size:9px; line-height:1.6; }.proposal code { color:#5fd7fa; font:500 8px 'IBM Plex Mono'; }.proposal>button { grid-column:1/3; height:33px; border:1px solid #2d715e; background:#153b32; color:#8ce9c5; border-radius:5px; cursor:pointer; font-size:9px; }.assistant-safe { margin-top:24px; padding:14px; border-radius:7px; background:#0b2131; display:flex; gap:10px; color:#5f91a8; }.assistant-safe svg { width:17px; }.assistant-safe span { display:grid; font-size:8px; line-height:1.5; }.assistant-safe strong { color:#91bdce; font-size:9px; }.toast { position:fixed; right:24px; bottom:24px; z-index:70; min-height:42px; padding:0 15px; display:flex; align-items:center; gap:8px; border:1px solid #2c715d; background:#102d27; color:#91e5c5; border-radius:7px; box-shadow:0 12px 35px rgba(0,0,0,.35); font-size:10px; }.toast svg { width:15px; }
+.full-state { min-height:100vh; display:flex; align-items:center; justify-content:center; gap:10px; color:#7f9bb0; font-size:12px; }.full-state svg { width:20px; }.full-state.error { color:#ff8293; }.spin { animation:spin 1s linear infinite; } @keyframes spin { to { transform:rotate(360deg); } }
+@media(max-width:900px){.sidebar{width:72px;padding-inline:10px}.brand>span:last-child,.nav-main span,.sidebar-foot{display:none}.brand{padding-inline:8px}.nav-main button{justify-content:center}.main-shell{margin-left:72px;width:calc(100% - 72px)}.content-head{align-items:flex-start}.head-meta{display:none}.edge-filter{display:none}.workflow-step{grid-template-columns:38px 1fr 60px}.step-progress{display:none}}
+@media(max-width:620px){.sidebar{display:none}.main-shell{margin-left:0;width:100%}.topbar{padding:0 14px}.breadcrumb{max-width:130px}.ghost-button{display:none!important}.content-head{padding:22px 16px 18px}.content-head h1{font-size:21px}.view-tabs{padding:0 10px}.canvas-area{padding:10px 5px}.edit-banner{margin:10px}.version{display:none}.widget-card{border-radius:7px}.workflow-layout{padding:0}.workflow-hero{margin:0 5px 10px;padding:18px}.workflow-hero h2{font-size:14px}.progress-ring{width:66px;height:66px}.workflow-steps{padding-inline:10px}.workflow-step{gap:7px}.status-badge{min-width:50px}.top-actions .edit-button{padding:0 9px}.top-actions .edit-button svg{display:none}}
*** End Patch
